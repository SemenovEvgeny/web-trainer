# 🚀 Быстрый деплой на домен

## За 3 шага:

### 1. Настройте конфигурацию

```bash
cp deploy-config.example .deploy-config
```

Откройте `.deploy-config` и заполните ваши данные от reg.ru:
- FTP_SERVER
- FTP_USER  
- FTP_PASS
- FTP_PATH (обычно `/public_html`)
- DOMAIN

### 2. Запустите деплой

```bash
npm run deploy:ftp
```

Или через SSH:
```bash
npm run deploy:ssh
```

### 3. Готово! 🎉

Приложение доступно на вашем домене!

---

## Команды деплоя

```bash
# Основной скрипт (только сборка)
npm run deploy

# Деплой через FTP
npm run deploy:ftp

# Деплой через SSH
npm run deploy:ssh

# Деплой для production
npm run deploy:production

# Деплой для staging
npm run deploy:staging
```

---

## Получение данных от reg.ru

1. Войдите в [панель reg.ru](https://www.reg.ru/user/login)
2. **FTP данные**: "Хостинг" → "FTP доступ"
3. **SSH данные**: "Хостинг" → "SSH доступ"

---

Подробнее: [DEPLOY_SCRIPTS.md](./DEPLOY_SCRIPTS.md)

