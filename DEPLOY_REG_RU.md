# Инструкция по деплою на reg.ru через GitHub

## Шаг 1: Подготовка проекта для GitHub

### 1.1. Инициализация Git репозитория

```bash
# Перейдите в папку проекта
cd /Users/evgenysemenov/git/web/test-web

# Инициализируем git репозиторий
git init

# Добавляем все файлы
git add .

# Делаем первый коммит
git commit -m "Initial commit: Trainer app"
```

### 1.2. Создание репозитория на GitHub

1. Перейдите на [GitHub.com](https://github.com)
2. Нажмите кнопку "+" в правом верхнем углу → "New repository"
3. Заполните форму:
   - **Repository name**: `trainer-app` (или любое другое имя)
   - **Description**: "Система управления тренировками"
   - Выберите **Public** или **Private**
   - **НЕ** создавайте README, .gitignore или license (они уже есть)
4. Нажмите "Create repository"

### 1.3. Подключение локального репозитория к GitHub

```bash
# Добавляем remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/trainer-app.git

# Переименовываем ветку в main
git branch -M main

# Отправляем код на GitHub
git push -u origin main
```

**Если GitHub запросит авторизацию:**
- Используйте Personal Access Token вместо пароля
- Создать токен: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
- Права: `repo` (полный доступ к репозиториям)

---

## Шаг 2: Настройка хостинга на reg.ru

### 2.1. Подготовка к деплою

Сначала соберите проект локально:

```bash
# Установите зависимости (если еще не установлены)
npm install

# Соберите проект для продакшена
npm run build
```

После сборки в папке `dist` будут готовые файлы для загрузки на сервер.

### 2.2. Вариант A: Деплой через файловый менеджер reg.ru

#### Шаг 1: Вход в панель управления

1. Войдите в [личный кабинет reg.ru](https://www.reg.ru/user/login)
2. Перейдите в раздел "Хостинг" → выберите ваш тариф
3. Откройте "Файловый менеджер"

#### Шаг 2: Загрузка файлов

1. Перейдите в папку `public_html` (или `www`, в зависимости от тарифа)
2. Удалите все файлы по умолчанию (если есть)
3. Загрузите все файлы из папки `dist`:
   - Выберите все файлы из `dist`
   - Загрузите через "Загрузить файлы" в файловом менеджере
   - Или используйте FTP/SFTP клиент (FileZilla, WinSCP)

#### Шаг 3: Настройка .htaccess для SPA

Создайте файл `.htaccess` в корне `public_html` со следующим содержимым:

```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>

# Кэширование статических файлов
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType image/jpg "access plus 1 year"
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/gif "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType text/css "access plus 1 month"
  ExpiresByType application/javascript "access plus 1 month"
</IfModule>

# Gzip сжатие
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript
</IfModule>
```

### 2.3. Вариант B: Деплой через SSH (рекомендуется)

#### Шаг 1: Получение данных для SSH

1. В панели reg.ru перейдите в "Хостинг" → "SSH доступ"
2. Включите SSH доступ (если не включен)
3. Запишите:
   - **SSH хост**: обычно `ssh.reg.ru` или `ваш-домен.reg.ru`
   - **SSH порт**: обычно `22`
   - **Логин**: ваш логин от хостинга
   - **Пароль**: ваш пароль от хостинга

#### Шаг 2: Подключение по SSH

```bash
# Подключитесь к серверу
ssh ваш-логин@ssh.reg.ru

# Или с указанием порта
ssh -p 22 ваш-логин@ssh.reg.ru
```

#### Шаг 3: Клонирование репозитория на сервер

```bash
# Перейдите в папку public_html
cd ~/public_html

# Клонируйте репозиторий (если есть доступ к Git на сервере)
git clone https://github.com/YOUR_USERNAME/trainer-app.git .

# Или создайте папку и скопируйте файлы
mkdir trainer-app
cd trainer-app
git clone https://github.com/YOUR_USERNAME/trainer-app.git .
```

#### Шаг 4: Установка Node.js на сервере (если нужно)

```bash
# Проверьте, установлен ли Node.js
node --version

# Если не установлен, установите через nvm (Node Version Manager)
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
source ~/.bashrc
nvm install 18
nvm use 18
```

#### Шаг 5: Сборка проекта на сервере

```bash
# Перейдите в папку проекта
cd ~/public_html/trainer-app

# Установите зависимости
npm install

# Соберите проект
npm run build

# Скопируйте файлы из dist в public_html
cp -r dist/* ~/public_html/
```

#### Шаг 6: Создание .htaccess

```bash
# Создайте .htaccess в public_html
cd ~/public_html
nano .htaccess
```

Вставьте содержимое из раздела 2.2, шаг 3, сохраните (Ctrl+O, Enter, Ctrl+X).

### 2.4. Вариант C: Автоматический деплой через GitHub Actions

#### Шаг 1: Создание workflow файла

Создайте файл `.github/workflows/deploy-regru.yml`:

```yaml
name: Deploy to reg.ru

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Build project
      run: npm run build
    
    - name: Deploy to reg.ru via FTP
      uses: SamKirkland/FTP-Deploy-Action@4.3.0
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        local-dir: ./dist/
        server-dir: /public_html/
```

#### Шаг 2: Настройка секретов в GitHub

1. Перейдите в ваш репозиторий на GitHub
2. Settings → Secrets and variables → Actions
3. Добавьте секреты:
   - `FTP_SERVER`: FTP сервер reg.ru (обычно `ftp.ваш-домен.ru`)
   - `FTP_USERNAME`: ваш логин от хостинга
   - `FTP_PASSWORD`: ваш пароль от хостинга

#### Шаг 3: Получение FTP данных

1. В панели reg.ru: "Хостинг" → "FTP доступ"
2. Найдите:
   - FTP сервер
   - FTP логин
   - FTP пароль

#### Шаг 4: Автоматический деплой

После каждого `git push` в ветку `main` GitHub Actions автоматически:
1. Соберет проект
2. Загрузит файлы на сервер reg.ru

---

## Шаг 3: Настройка домена

### 3.1. Подключение домена (если еще не подключен)

1. В панели reg.ru: "Домены" → выберите ваш домен
2. Настройте DNS записи:
   - **A запись**: укажите IP адрес сервера reg.ru
   - **CNAME**: `www` → ваш домен

### 3.2. Проверка работы

1. Откройте ваш домен в браузере
2. Приложение должно загрузиться
3. Если видите ошибку 404, проверьте:
   - Правильность пути к файлам
   - Наличие файла `.htaccess`
   - Настройки mod_rewrite на сервере

---

## Шаг 4: Обновление приложения

### Ручное обновление:

```bash
# Локально: внесите изменения, соберите проект
npm run build

# Загрузите новые файлы из dist на сервер через FTP/SSH
```

### Автоматическое обновление (с GitHub Actions):

```bash
# Локально: внесите изменения
git add .
git commit -m "Описание изменений"
git push
```

GitHub Actions автоматически задеплоит изменения на reg.ru.

---

## Полезные команды

### Локальная разработка:

```bash
# Запуск dev-сервера
npm run dev

# Сборка для продакшена
npm run build

# Просмотр собранного проекта
npm run preview
```

### Работа с Git:

```bash
# Проверить статус
git status

# Добавить изменения
git add .

# Сделать коммит
git commit -m "Описание изменений"

# Отправить на GitHub
git push

# Получить изменения с GitHub
git pull
```

### Работа с сервером (SSH):

```bash
# Подключиться к серверу
ssh ваш-логин@ssh.reg.ru

# Просмотреть файлы
ls -la

# Редактировать файл
nano filename

# Удалить файл
rm filename

# Создать папку
mkdir folder-name

# Перейти в папку
cd folder-name
```

---

## Решение проблем

### Проблема: Белый экран или 404 ошибка

**Решение:**
1. Проверьте, что файл `index.html` находится в корне `public_html`
2. Убедитесь, что файл `.htaccess` создан и содержит правильные правила
3. Проверьте права доступа к файлам (должны быть 644 для файлов, 755 для папок)

### Проблема: Ошибка при сборке проекта

**Решение:**
```bash
# Очистите кэш и переустановите зависимости
rm -rf node_modules package-lock.json
npm install
npm run build
```

### Проблема: Не работает роутинг (404 при переходе по ссылкам)

**Решение:**
- Убедитесь, что файл `.htaccess` создан и содержит правила для SPA
- Проверьте, что mod_rewrite включен на сервере (обратитесь в поддержку reg.ru)

### Проблема: Медленная загрузка

**Решение:**
- Включите Gzip сжатие в `.htaccess`
- Настройте кэширование статических файлов
- Используйте CDN для статических ресурсов

---

## Контакты поддержки

- **Техподдержка reg.ru**: https://www.reg.ru/support/
- **Документация reg.ru**: https://www.reg.ru/support/help/
- **GitHub Support**: https://support.github.com/

---

## Дополнительные настройки

### Настройка SSL сертификата

1. В панели reg.ru: "Хостинг" → "SSL сертификаты"
2. Активируйте бесплатный Let's Encrypt сертификат
3. Приложение будет доступно по HTTPS

### Настройка резервного копирования

1. В панели reg.ru: "Хостинг" → "Резервное копирование"
2. Настройте автоматическое создание бэкапов
3. Рекомендуется: ежедневные бэкапы

---

## Итоговая схема деплоя

```
Локальная разработка
    ↓
Git commit & push
    ↓
GitHub репозиторий
    ↓
GitHub Actions (автоматически)
    ↓
Сборка проекта (npm run build)
    ↓
Загрузка на reg.ru (FTP/SSH)
    ↓
Приложение доступно на вашем домене
```

## Готовые файлы в проекте

В проекте уже есть примеры файлов:

1. **`.htaccess.example`** - пример файла для настройки SPA роутинга
   - Скопируйте в `public_html` на сервере и переименуйте в `.htaccess`

2. **`.github/workflows/deploy-regru.yml.example`** - пример workflow для автоматического деплоя
   - Скопируйте в `.github/workflows/deploy-regru.yml` для автоматизации

Готово! 🎉

