# Скрипты для деплоя приложения

В проекте есть несколько скриптов для упрощения процесса деплоя на домен.

## Доступные скрипты

### 1. `deploy.sh` - Основной скрипт деплоя

Универсальный скрипт для сборки и подготовки к деплою.

**Использование:**
```bash
# Сборка для продакшена
./deploy.sh
# или
npm run deploy

# Сборка для staging
./deploy.sh staging
# или
npm run deploy:staging
```

**Что делает:**
- ✅ Проверяет наличие Node.js и npm
- ✅ Устанавливает зависимости (`npm ci`)
- ✅ Собирает проект (`npm run build`)
- ✅ Проверяет наличие папки `dist`
- ✅ Показывает размер собранного проекта
- ✅ Выводит инструкции для дальнейших действий
- ✅ Опционально: автоматическая загрузка через FTP (если настроено)

### 2. `deploy-ftp.sh` - Деплой через FTP

Автоматический деплой на сервер через FTP.

**Использование:**
```bash
./deploy-ftp.sh
# или
npm run deploy:ftp
```

**Требования:**
- Установленный `lftp`: `brew install lftp` (macOS) или `apt-get install lftp` (Linux)
- Настроенный файл `.deploy-config`

**Что делает:**
- ✅ Собирает проект
- ✅ Загружает файлы из `dist` на сервер через FTP
- ✅ Загружает `.htaccess` файл
- ✅ Удаляет старые файлы на сервере

### 3. `deploy-ssh.sh` - Деплой через SSH

Автоматический деплой на сервер через SSH.

**Использование:**
```bash
./deploy-ssh.sh
# или
npm run deploy:ssh
```

**Требования:**
- Настроенный SSH доступ к серверу
- Настроенный файл `.deploy-config`

**Что делает:**
- ✅ Собирает проект
- ✅ Создает архив из папки `dist`
- ✅ Загружает архив на сервер через SSH
- ✅ Распаковывает архив на сервере
- ✅ Настраивает права доступа (755 для папок, 644 для файлов)
- ✅ Создает файл `.htaccess`

## Настройка конфигурации

### Шаг 1: Создайте файл конфигурации

```bash
cp deploy-config.example .deploy-config
```

### Шаг 2: Заполните данные

Откройте `.deploy-config` и заполните:

```bash
# FTP настройки для reg.ru
FTP_SERVER=ftp.your-domain.ru
FTP_USER=your-ftp-username
FTP_PASS=your-ftp-password
FTP_PATH=/public_html

# SSH настройки (альтернатива FTP)
SSH_HOST=ssh.your-domain.ru
SSH_USER=your-ssh-username
SSH_PATH=/home/username/public_html

# Домен приложения
DOMAIN=https://your-domain.ru
```

**Важно:** Файл `.deploy-config` уже добавлен в `.gitignore` и не будет загружен в Git!

## Быстрый старт

### Вариант 1: Ручной деплой (рекомендуется для начала)

```bash
# 1. Соберите проект
npm run deploy

# 2. Загрузите файлы из папки dist на сервер вручную через FTP/SFTP
# 3. Загрузите .htaccess.example как .htaccess в корень public_html
```

### Вариант 2: Автоматический деплой через FTP

```bash
# 1. Настройте .deploy-config
cp deploy-config.example .deploy-config
# Отредактируйте .deploy-config

# 2. Установите lftp (если еще не установлен)
brew install lftp  # macOS
# или
sudo apt-get install lftp  # Linux

# 3. Запустите деплой
npm run deploy:ftp
```

### Вариант 3: Автоматический деплой через SSH

```bash
# 1. Настройте .deploy-config
cp deploy-config.example .deploy-config
# Отредактируйте .deploy-config

# 2. Настройте SSH ключи (рекомендуется)
ssh-copy-id user@your-server.com

# 3. Запустите деплой
npm run deploy:ssh
```

## Получение данных для деплоя (reg.ru)

### FTP данные:

1. Войдите в [панель reg.ru](https://www.reg.ru/user/login)
2. Перейдите: "Хостинг" → "FTP доступ"
3. Найдите:
   - **FTP сервер**: обычно `ftp.ваш-домен.ru`
   - **FTP логин**: ваш логин
   - **FTP пароль**: ваш пароль

### SSH данные:

1. В панели reg.ru: "Хостинг" → "SSH доступ"
2. Включите SSH доступ (если не включен)
3. Найдите:
   - **SSH хост**: обычно `ssh.reg.ru` или `ваш-домен.reg.ru`
   - **SSH порт**: обычно `22`
   - **Логин**: ваш логин от хостинга
   - **Пароль**: ваш пароль от хостинга

## Примеры использования

### Деплой после изменений

```bash
# 1. Внесите изменения в код
# 2. Соберите и задеплойте
npm run deploy:ftp

# Или если используете SSH
npm run deploy:ssh
```

### Деплой только сборки (без загрузки)

```bash
npm run deploy
# Затем загрузите файлы из dist вручную
```

### Проверка перед деплоем

```bash
# Соберите проект
npm run build

# Просмотрите результат локально
npm run preview

# Если все хорошо, задеплойте
npm run deploy:ftp
```

## Решение проблем

### Ошибка: "lftp не установлен"

**Решение:**
```bash
# macOS
brew install lftp

# Linux (Ubuntu/Debian)
sudo apt-get install lftp

# Linux (CentOS/RHEL)
sudo yum install lftp
```

### Ошибка: "Permission denied" при SSH

**Решение:**
1. Настройте SSH ключи:
```bash
ssh-copy-id user@your-server.com
```

2. Или используйте FTP вместо SSH

### Ошибка: "FTP connection failed"

**Решение:**
1. Проверьте правильность данных в `.deploy-config`
2. Убедитесь, что FTP доступ включен в панели reg.ru
3. Проверьте, что сервер доступен:
```bash
ping ftp.your-domain.ru
```

### Ошибка: "File not found" на сайте

**Решение:**
1. Убедитесь, что файл `.htaccess` загружен на сервер
2. Проверьте права доступа:
   - Файлы: 644
   - Папки: 755
3. Убедитесь, что файлы загружены в правильную папку (`public_html`)

## Безопасность

⚠️ **Важно:**
- Никогда не коммитьте файл `.deploy-config` в Git (он уже в `.gitignore`)
- Не делитесь файлом `.deploy-config` с другими
- Используйте SSH ключи вместо паролей, когда это возможно
- Регулярно меняйте пароли доступа

## Интеграция с GitHub Actions

Для автоматического деплоя при каждом push в GitHub используйте файл:
`.github/workflows/deploy-regru.yml.example`

Скопируйте его в `.github/workflows/deploy-regru.yml` и настройте секреты в GitHub.

---

## Итоговая схема работы

```
Локальная разработка
    ↓
npm run deploy:ftp (или deploy:ssh)
    ↓
Сборка проекта (npm run build)
    ↓
Загрузка на сервер (FTP/SSH)
    ↓
Приложение доступно на домене
```

Готово! 🚀

