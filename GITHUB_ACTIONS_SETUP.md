# 🚀 Настройка GitHub Actions

## Быстрая настройка для reg.ru

### Шаг 1: Добавьте секреты в GitHub

1. Перейдите в ваш репозиторий: https://github.com/SemenovEvgeny/web-trainer
2. Нажмите **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**
4. Добавьте следующие секреты:

```
FTP_SERVER=ftp.your-domain.ru
FTP_USERNAME=your-ftp-username
FTP_PASSWORD=your-ftp-password
FTP_PATH=/public_html
DOMAIN=https://your-domain.ru
```

### Шаг 2: Готово! 🎉

Теперь при каждом `git push` в ветку `main`:
- ✅ Проект автоматически соберется
- ✅ Файлы загрузятся на сервер reg.ru
- ✅ Приложение обновится на вашем домене

---

## Доступные workflows

### 1. CI (Проверка кода)
**Файл:** `.github/workflows/ci.yml`

Автоматически запускается при:
- Создании Pull Request
- Push в ветки `main` или `develop`

**Что делает:**
- Проверяет код линтером
- Собирает проект
- Сохраняет артефакты

### 2. Deploy to reg.ru
**Файл:** `.github/workflows/deploy-regru.yml`

Автоматически запускается при:
- Push в ветку `main`
- Ручном запуске через GitHub UI

**Что делает:**
- Собирает проект
- Загружает на reg.ru через FTP
- Загружает `.htaccess`

### 3. Deploy to Vercel
**Файл:** `.github/workflows/deploy-vercel.yml`

**Требуемые секреты:**
- `VERCEL_TOKEN`
- `VERCEL_ORG_ID`
- `VERCEL_PROJECT_ID`

### 4. Deploy to Netlify
**Файл:** `.github/workflows/deploy-netlify.yml`

**Требуемые секреты:**
- `NETLIFY_AUTH_TOKEN`
- `NETLIFY_SITE_ID`

---

## Как получить данные для секретов

### FTP данные (reg.ru):

1. Войдите в [панель reg.ru](https://www.reg.ru/user/login)
2. Перейдите: **Хостинг** → **FTP доступ**
3. Скопируйте:
   - FTP сервер
   - FTP логин
   - FTP пароль

### Vercel данные:

1. Войдите в [Vercel](https://vercel.com)
2. **Settings** → **Tokens** → **Create Token**
3. Для получения ID: выполните `vercel link` локально

### Netlify данные:

1. Войдите в [Netlify](https://netlify.com)
2. **User settings** → **Applications** → **New access token**
3. Site ID в настройках сайта

---

## Ручной запуск workflow

1. Перейдите в **Actions** в вашем репозитории
2. Выберите нужный workflow (например, "Deploy to reg.ru")
3. Нажмите **Run workflow**
4. Выберите ветку и нажмите **Run workflow**

---

## Мониторинг

Все запуски можно посмотреть в разделе **Actions**:
https://github.com/SemenovEvgeny/web-trainer/actions

---

## Отключение автоматического деплоя

Если нужно временно отключить:

1. Перейдите в **Actions**
2. Выберите workflow
3. Нажмите **...** → **Disable workflow**

Или переименуйте файл workflow, добавив `.disabled` в конец.

---

## Проверка работы

После настройки секретов:

1. Сделайте небольшое изменение в коде
2. Закоммитьте и запушьте:
   ```bash
   git add .
   git commit -m "Test GitHub Actions"
   git push
   ```
3. Перейдите в **Actions** и посмотрите запуск workflow
4. Через несколько минут приложение обновится на сервере!

---

Подробнее: [.github/workflows/README.md](./.github/workflows/README.md)

