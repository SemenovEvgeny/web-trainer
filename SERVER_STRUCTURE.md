# Структура файлов на FTP сервере (reg.ru)

После успешного деплоя на сервер reg.ru структура файлов в папке `public_html` будет выглядеть следующим образом:

## 📁 Структура папки public_html

```
public_html/
├── index.html                    # Главный HTML файл приложения
├── .htaccess                     # Конфигурация Apache для SPA роутинга
└── assets/                       # Папка со статическими ресурсами
    ├── index-XXXXXXXX.js         # JavaScript файл приложения (с хешем)
    └── index-XXXXXXXX.css        # CSS файл стилей (с хешем)
```

## 📄 Описание файлов

### 1. `index.html` (главный файл)

Это точка входа в приложение. Содержит:

```html
<!doctype html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Система управления тренировками</title>
    <script type="module" crossorigin src="/assets/index-XXXXX.js"></script>
    <link rel="stylesheet" crossorigin href="/assets/index-XXXXX.css">
  </head>
  <body>
    <div id="root"></div>
  </body>
</html>
```

**Важно:** Все запросы к приложению должны возвращать этот файл (настроено через `.htaccess`).

---

### 2. `.htaccess` (конфигурация сервера)

Файл для настройки Apache сервера. Обеспечивает:

- ✅ Правильный роутинг для SPA (Single Page Application)
- ✅ Кэширование статических файлов
- ✅ Gzip сжатие
- ✅ Безопасность (заголовки)

**Содержимое:**
```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>

# Кэширование и сжатие...
```

---

### 3. `assets/` (папка со статическими ресурсами)

Содержит минифицированные и оптимизированные файлы:

#### `index-XXXXXXXX.js`
- Весь JavaScript код приложения
- React компоненты
- Логика приложения
- Размер: ~247 KB (сжато до ~73 KB с gzip)

#### `index-XXXXXXXX.css`
- Все стили приложения
- Tailwind CSS (только используемые классы)
- Размер: ~19 KB (сжато до ~4 KB с gzip)

**Примечание:** Имена файлов содержат хеш (например, `index-CS-Q3yW-.js`), который меняется при каждой сборке для правильного кэширования.

---

## 🔍 Как проверить структуру на сервере

### Через FTP клиент (FileZilla, WinSCP):

1. Подключитесь к серверу через FTP
2. Перейдите в папку `public_html`
3. Вы должны увидеть:
   - `index.html`
   - `.htaccess`
   - Папку `assets/` с файлами внутри

### Через SSH:

```bash
ssh ваш-логин@ssh.reg.ru
cd ~/public_html
ls -la
```

Вывод должен быть примерно таким:
```
total 280
drwxr-xr-x  3 user user   4096 Nov 15 17:33 .
drwxr-xr-x  5 user user   4096 Nov 15 10:00 ..
-rw-r--r--  1 user user    508 Nov 15 17:33 index.html
-rw-r--r--  1 user user   1234 Nov 15 17:33 .htaccess
drwxr-xr-x  2 user user   4096 Nov 15 17:33 assets
```

---

## 📊 Размеры файлов (примерные)

После деплоя размеры файлов будут примерно такими:

```
public_html/
├── index.html          ~0.5 KB
├── .htaccess           ~1.2 KB
└── assets/
    ├── index-XXX.js    ~247 KB (73 KB с gzip)
    └── index-XXX.css   ~20 KB (4 KB с gzip)
```

**Общий размер:** ~270 KB (без сжатия), ~80 KB (с gzip)

---

## ✅ Проверка работоспособности

### 1. Проверка файлов:

```bash
# На сервере через SSH
cd ~/public_html
ls -la
# Должны быть: index.html, .htaccess, assets/

ls -la assets/
# Должны быть: .js и .css файлы
```

### 2. Проверка через браузер:

1. Откройте ваш домен: `https://your-domain.ru`
2. Должна загрузиться страница входа
3. Откройте DevTools (F12) → Network
4. Проверьте, что все файлы загружаются (статус 200)

### 3. Проверка роутинга:

1. Перейдите на: `https://your-domain.ru/trainer`
2. Должна загрузиться страница тренера (не 404)
3. Это означает, что `.htaccess` работает правильно

---

## 🔧 Что делать, если файлов нет или они неполные

### Проблема: Файлы не загрузились

**Решение:**
1. Проверьте логи GitHub Actions
2. Убедитесь, что секреты настроены правильно
3. Попробуйте загрузить файлы вручную:
   ```bash
   npm run build
   # Затем загрузите содержимое dist/ через FTP
   ```

### Проблема: Белый экран

**Решение:**
1. Проверьте, что файл `.htaccess` загружен
2. Проверьте права доступа (644 для файлов, 755 для папок)
3. Проверьте консоль браузера на ошибки

### Проблема: 404 ошибка при переходе по ссылкам

**Решение:**
1. Убедитесь, что файл `.htaccess` существует
2. Проверьте, что mod_rewrite включен на сервере
3. Обратитесь в поддержку reg.ru

---

## 📝 Права доступа

Рекомендуемые права доступа:

```bash
# Файлы
chmod 644 index.html
chmod 644 .htaccess
chmod 644 assets/*.js
chmod 644 assets/*.css

# Папки
chmod 755 .
chmod 755 assets
```

---

## 🔄 После обновления

После каждого деплоя:

1. **Имена файлов в assets/ изменятся** (новый хеш)
2. **Старые файлы останутся** (можно удалить вручную)
3. **index.html обновится** с новыми путями к файлам
4. **.htaccess обычно не меняется**

Для очистки старых файлов можно использовать `dangerous-clean-slate: true` в workflow (осторожно!).

---

## 📸 Визуальное представление

```
public_html/
│
├── 📄 index.html          ← Точка входа, загружается первым
│
├── 📄 .htaccess           ← Настройки сервера (роутинг, кэш)
│
└── 📁 assets/             ← Статические ресурсы
    │
    ├── 📄 index-XXXXX.js  ← JavaScript приложения
    │   (247 KB → 73 KB gzip)
    │
    └── 📄 index-XXXXX.css ← Стили приложения
        (20 KB → 4 KB gzip)
```

---

## 🎯 Итог

После успешного деплоя на сервере должно быть:

- ✅ **index.html** - главный файл
- ✅ **.htaccess** - конфигурация сервера
- ✅ **assets/** - папка с JS и CSS файлами

Всего: **3 элемента** (1 файл, 1 конфиг, 1 папка с 2 файлами)

Приложение готово к работе! 🚀

